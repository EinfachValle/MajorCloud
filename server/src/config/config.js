const env = {
    PORT: 3030
}

const database = {
    HOSTNAME: 'localhost',
    PORT: '7000',
    USERNAME: 'nodeserver',
    PASSWORD: '#35bT-015',
    DATABASE: 'neatbeat',
    USERTABLE: 'accounts'
}

const mailer = {}

module.exports = { env, database, mailer }